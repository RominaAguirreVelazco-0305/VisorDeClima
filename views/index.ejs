<!DOCTYPE html> 
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visor del Clima</title>
    <!-- Agregar el favicon -->
    <link rel="icon" href="img/img10.png" type="image/png">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
    <style>

    body {
        
        font-family: 'Poppins', sans-serif;
        margin: 0;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        position: relative;
        overflow: hidden;

    }

    body::before {
        content: '';
        background-image: url('/img/IMG11.jpg'); /* Ruta de tu imagen */
        background-size: cover;
        background-position: center;
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        opacity: 0.2;
        z-index: -1;
    }

    .container {
        background-color: rgba(253, 227, 243, 0.9); 
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        text-align: center;
        width: 400px;
        z-index: 1;
        position: relative;
    }

    h1 {
        color: #f90606;
        font-size: 2.5em;
        margin-bottom: 20px;
    }

    form {
        display: flex;
        justify-content: center;
        margin-bottom: 20px;
    }

    input[type="text"] {
        padding: 10px;
        font-size: 1.2em;
        border-radius: 5px;
        border: 1px solid #ddd;
        margin-right: 10px;
        width: 70%;
    }

    button {
        background-color: #1a5be8;
        color: white;
        padding: 10px 15px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1.1em;
        transition: transform 0.2s;
    }

    button:hover {
        background-color: #FFB800;
        transform: scale(1.1);
    }

    .weather-info {
        font-size: 1.5em;
        color: #333;
        margin-top: 20px;
    }

    .weather-info img {
        width: 80px;
        height: 80px;
        transition: transform 0.5s, filter 0.5s;
        cursor: pointer;
    }

    .weather-info img:hover {
        transform: rotate(360deg) scale(1.2);
        filter: brightness(1.5);
    }

    .error {
        color: rgb(17, 74, 243);
        margin-top: 20px;
        font-size: 1.2em;
    }

    .hidden {
        display: none;
    }

    .footer {
        margin-top: 30px;
        font-size: 0.9em;
        color: #777;
    }

    .footer a {
        color: #FF6F61;
    }
    </style>
</head>
<body>
    <div class="container">
        <h1>Visor del Clima <span class="icono-emoji">🌦️</span></h1>
        <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">


        <!-- Formulario para ingresar la ciudad -->
        <form action="/getWeather" method="get" onsubmit="showLoading()">
            <input type="text" name="city" id="city" placeholder=" 🌇 Ingresa tu ciudad" required>
            <button type="submit">Obtener Clima 🌤️</button>
        </form>

        <!-- Mensaje de carga -->
        <div id="loading" class="hidden">
            <p>Cargando... <span class="icono-emoji">⏳</span></p>
        </div>

        <!-- Información del clima -->
        <% if (weather) { %>
            <div class="weather-info">
                <p>🏙️ Ciudad: <%= weather.city %></p>
                <p>🌡️ Temperatura: <%= weather.temperature %>°C</p>
                <p>🌦️ Descripción: <%= weather.description %></p>
                <img src="<%= weather.icon %>" alt="Icono del clima">
            </div>
        <% } %>

        <!-- Mostrar emojis dependiendo del clima -->
        <p>Pronóstico: <span class="emoji-clima">🌥️ 🌧️ 🌪️ 🌩️</span></p>

        <% if (error) { %>
            <p class="error">⚠️ <%= error %></p>
        <% } %>

        <!-- Pie de página -->
        <div class="footer">
            <p>Desarrollado por <a href="#">Romina Aguirre</a></p>
        </div>
    </div>

    <script>
        // Mostrar el mensaje de carga cuando se envía el formulario
        function showLoading() {
            document.getElementById('loading').classList.remove('hidden');
        }
    </script>
</body>
</html>
